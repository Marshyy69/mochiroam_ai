name: mochiroam_ai
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1

  flutter_svg: ^2.2.3

  flutter_chat_core: 2.9.0
  flutter_chat_ui: ^2.11.0
  flutter_chat_types: ^3.6.2
  uuid: ^4.2.2

  dash_chat_2: ^0.0.19


  # HTTP & API
  http: ^1.6.0

  # Connectivity (latest version)
  connectivity_plus: ^7.0.0

  # Secure storage
  flutter_secure_storage: ^10.0.0

  # Firebase latest (matching your SDK)
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0
  cloud_functions: ^6.0.4

  # Local storage
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
